# DB 저장 + 인증 구현 계획 (로드맵 2번)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **체크박스 갱신 규칙**: Task·Step을 끝내면 그때그때 `- [ ]`→`- [x]`로 바꿀 것(커밋·PR과 함께). 문서와 진행이 어긋나지 않게. → conventions.md §6.

**Goal:** 로그인한 사용자의 지식 그래프를 서버 DB(Supabase Postgres)에 저장해 여러 기기에서 같은 그래프를 쓰게 한다. 비로그인은 예시 그래프만 본다.

**Architecture:** 하이브리드 — 브라우저는 직접 쓴 Next.js API 라우트(`/api/graph`)를 거치고, 라우트는 쿠키 세션으로 사용자를 확인해 supabase-js로 그 사람의 `nodes`·`edges` 행만 조회/저장한다. 저장소 전환은 `storage.ts` 한 곳에서 로그인 여부로 갈린다(로그인→서버 / 비로그인→예시).

**Tech Stack:** Next.js 16 · React 19 · TypeScript 5 · Vitest 4 · **@supabase/supabase-js · @supabase/ssr**(쿠키 세션) · 정규화 스키마 + RLS · 소셜 로그인(1차 Google·Kakao).

**참고 문서:** 설계 `docs/specs/2026-06-08-db-auth-design.md` · [ADR 0011](../decisions/0011-db-auth-architecture.md) · [ADR 0010](../decisions/0010-db-auth-priority.md)

**브랜치 규칙:** 먼저 `/create-issue`로 이슈(로드맵 2 DB+인증)+브랜치 생성 → 브랜치명 예 `feat/N-db-auth`. 커밋은 Conventional Commits **scope 없이** `type: 한국어 제목 (#N)`. main 직접 커밋 금지.

> ⚠️ **이 계획의 컴포넌트·로직 코드는 사용자가 직접 타이핑**한다(프로젝트 규칙). 아래 코드 블록은 그대로 옮겨 쓸 참조본이다.
>
> ⚠️ **자동 테스트가 닿는 곳은 순수 함수(Task 3)뿐**이다. 인증·라우트·OAuth·쿠키는 자동 테스트로 못 잡으니 Task 8의 **수동 검수**가 진짜 게이트다("테스트 통과 ≠ 동작").

---

## 선행: Supabase·소셜 앱 셋업 (사용자가 직접) — Task 1

비밀값·외부 콘솔 작업이라 **사용자가 직접** 수행한다. Claude는 절차·SQL만 제공.

### Task 1: Supabase 프로젝트 + 표 + RLS + 소셜 앱 + 환경변수

**Files:** `.env.local`(신규, gitignore됨) · Supabase 대시보드(코드 아님)

- [x] **Step 1: 이슈 + 브랜치 생성**

`/create-issue`로 "DB 저장 + 인증(로드맵 2)" 이슈와 작업 브랜치를 만든다. 이후 커밋에 `(#N)`을 붙인다.

- [x] **Step 2: Supabase 프로젝트 생성** (사용자)

[supabase.com](https://supabase.com) 로그인 → New project. 상단 **Connect** 버튼 → App Frameworks → Next.js에서 **Project URL**과 **Publishable key**(`sb_publishable_…`, 옛 anon 자리)를 복사(둘 다 공개용, 비밀 아님). **Secret key**(`sb_secret_…`, 옛 service_role)는 **쓰지 않으니 복사·노출하지 말 것**.

- [x] **Step 3: 표 + RLS 만들기** (사용자 — Supabase SQL Editor에 붙여 실행)

```sql
-- 점(노드) 표
create table public.nodes (
  user_id uuid not null references auth.users (id) on delete cascade,
  id      text not null,
  label   text not null,
  topic   text not null,
  primary key (user_id, id)
);

-- 선(엣지) 표
create table public.edges (
  user_id uuid not null references auth.users (id) on delete cascade,
  id      text not null,
  source  text not null,
  target  text not null,
  primary key (user_id, id)
);

-- 행 수준 보안: 자기 user_id 행만 읽기/쓰기
alter table public.nodes enable row level security;
alter table public.edges enable row level security;

create policy "own nodes" on public.nodes
  for all using (auth.uid() = user_id) with check (auth.uid() = user_id);
create policy "own edges" on public.edges
  for all using (auth.uid() = user_id) with check (auth.uid() = user_id);

-- 그래프 통째 교체를 한 묶음으로 처리(전부 되거나 전부 취소). 삭제+삽입이 중간에 끊겨도 반쪽이 안 남음 → 반쪽 저장 차단.
-- user_id는 함수 안에서 auth.uid()로 채운다(클라이언트가 보낸 user_id를 믿지 않음).
create or replace function replace_graph(p_nodes jsonb, p_edges jsonb)
returns void as $$
begin
  delete from edges where user_id = auth.uid();
  delete from nodes where user_id = auth.uid();
  insert into nodes (user_id, id, label, topic)
    select auth.uid(), x.id, x.label, x.topic
    from jsonb_to_recordset(p_nodes) as x(id text, label text, topic text);
  insert into edges (user_id, id, source, target)
    select auth.uid(), x.id, x.source, x.target
    from jsonb_to_recordset(p_edges) as x(id text, source text, target text);
end;
$$ language plpgsql security definer;
```

- [x] **Step 4: 소셜 로그인 제공자 설정** (사용자 — Google 완료, Kakao 후속)

Supabase 대시보드 → Authentication → Providers에서 **Google**, **Kakao**를 켜고 각 개발자 콘솔에서 발급한 Client ID/Secret 입력. 각 콘솔의 **Authorized redirect URI**에 Supabase가 안내하는 콜백 주소(`https://<프로젝트>.supabase.co/auth/v1/callback`)를 등록. (Google: Google Cloud Console / Kakao: Kakao Developers)

- [x] **Step 5: 로컬 환경변수**

`.env.local` 생성(이미 `.gitignore`에 `.env*` 포함 확인):

```
NEXT_PUBLIC_SUPABASE_URL=https://<프로젝트>.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=<publishable key (sb_publishable_…)>
```

- [ ] **Step 6: 커밋** (코드 변경 없음 — 셋업 단계라 커밋 생략 가능. 다음 Task부터 커밋)

---

### Task 2: 패키지 설치 + Supabase 클라이언트 + 세션 미들웨어

**Files:**
- 설치: `@supabase/supabase-js`, `@supabase/ssr`
- Create: `src/lib/supabase/client.ts`, `src/lib/supabase/server.ts`, `src/middleware.ts`

- [x] **Step 1: 패키지 설치**

Run: `npm install @supabase/supabase-js @supabase/ssr`

- [x] **Step 2: 브라우저용 클라이언트**

`src/lib/supabase/client.ts`:

```ts
import { createBrowserClient } from '@supabase/ssr'

// 브라우저(클라이언트 컴포넌트)에서 쓰는 supabase 클라이언트
export function createClient() {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!
  )
}
```

- [x] **Step 3: 서버용 클라이언트 (쿠키 세션 읽기)**

`src/lib/supabase/server.ts`:

```ts
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'

// 서버(라우트 핸들러)에서 쓰는 클라이언트 — 쿠키로 세션을 읽는다.
// Next 16: cookies()는 async라 await 필요.
export async function createClient() {
  const cookieStore = await cookies()
  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!,
    {
      cookies: {
        getAll() {
          return cookieStore.getAll()
        },
        setAll(cookiesToSet) {
          // 라우트 핸들러에선 쿠키 쓰기가 허용. 서버 컴포넌트에서 부르면 막힐 수 있어 try로 감쌈.
          try {
            cookiesToSet.forEach(({ name, value, options }) => cookieStore.set(name, value, options))
          } catch {}
        },
      },
    }
  )
}
```

- [x] **Step 4: 세션 갱신 미들웨어**

`src/middleware.ts` (만료 직전 토큰을 새로 고쳐 쿠키를 갱신 — @supabase/ssr 표준 패턴):

```ts
import { type NextRequest, NextResponse } from 'next/server'
import { createServerClient } from '@supabase/ssr'

export async function middleware(request: NextRequest) {
  let response = NextResponse.next({ request })

  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!,
    {
      cookies: {
        getAll() {
          return request.cookies.getAll()
        },
        setAll(cookiesToSet) {
          cookiesToSet.forEach(({ name, value }) => request.cookies.set(name, value))
          response = NextResponse.next({ request })
          cookiesToSet.forEach(({ name, value, options }) => response.cookies.set(name, value, options))
        },
      },
    }
  )

  // 세션을 한 번 읽어 토큰을 갱신(쿠키 재설정).
  await supabase.auth.getUser()
  return response
}

export const config = {
  // api 라우트는 제외 — 라우트가 스스로 getUser()로 인증하므로, 미들웨어의 중복 세션 확인(원격 왕복)을 피한다.
  // 토큰 갱신은 페이지 이동 때 미들웨어가 처리하고, 브라우저 클라이언트도 백그라운드로 갱신한다.
  matcher: ['/((?!_next/static|_next/image|favicon.ico|api).*)'],
}
```

- [x] **Step 5: 타입체크**

Run: `npx tsc --noEmit`
Expected: 통과 (에러 없음)

- [x] **Step 6: 커밋**

```bash
git add package.json package-lock.json src/lib/supabase src/middleware.ts
git commit -m "feat: supabase 클라이언트·세션 미들웨어 추가 (#N)"
```

---

### Task 3: DB 행 → 그래프 변환 (순수 함수, TDD)

> RPC가 저장 시 `user_id`를 채우므로 저장 방향 변환(`graphToRows`)은 불필요. 불러오기 방향 `rowsToGraph`만 만든다.

**Files:**
- Create: `src/lib/graph-rows.ts`
- Test: `src/lib/graph-rows.test.ts`

- [x] **Step 1: 실패하는 테스트 작성**

`src/lib/graph-rows.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { rowsToGraph } from './graph-rows'

describe('rowsToGraph', () => {
  it('DB 행(점·선)을 Graph 모양으로 합친다', () => {
    const nodeRows = [
      { id: 'n1', label: 'useState', topic: 'React' },
      { id: 'n2', label: 'TDD', topic: '테스트' },
    ]
    const edgeRows = [{ id: 'n1-n2', source: 'n1', target: 'n2' }]
    expect(rowsToGraph(nodeRows, edgeRows)).toEqual({
      nodes: [
        { id: 'n1', label: 'useState', topic: 'React' },
        { id: 'n2', label: 'TDD', topic: '테스트' },
      ],
      edges: [{ id: 'n1-n2', source: 'n1', target: 'n2' }],
    })
  })

  it('빈 행은 빈 그래프', () => {
    expect(rowsToGraph([], [])).toEqual({ nodes: [], edges: [] })
  })
})
```

- [x] **Step 2: 실패 확인**

Run: `npx vitest run src/lib/graph-rows.test.ts`
Expected: FAIL ("rowsToGraph is not defined" 류 — 모듈 없음)

- [x] **Step 3: 최소 구현**

`src/lib/graph-rows.ts`:

```ts
import type { Graph } from './types'

// DB 행 → 그래프 (화면이 쓰는 모양으로). 저장 방향은 RPC가 처리하므로 불필요.
export function rowsToGraph(
  nodeRows: { id: string; label: string; topic: string }[],
  edgeRows: { id: string; source: string; target: string }[]
): Graph {
  return {
    nodes: nodeRows.map((row) => ({ id: row.id, label: row.label, topic: row.topic })),
    edges: edgeRows.map((row) => ({ id: row.id, source: row.source, target: row.target })),
  }
}
```

- [x] **Step 4: 통과 확인**

Run: `npx vitest run src/lib/graph-rows.test.ts`
Expected: PASS (2개 통과)

- [x] **Step 5: 커밋**

```bash
git add src/lib/graph-rows.ts src/lib/graph-rows.test.ts
git commit -m "feat: 행→그래프 변환 순수 함수 추가 (TDD) (#N)"
```

---

### Task 4: `/api/graph` 라우트 (GET/PUT)

**Files:**
- Create: `src/app/api/graph/route.ts`

- [ ] **Step 1: 라우트 작성**

`src/app/api/graph/route.ts`:

```ts
import { NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'
import { rowsToGraph } from '@/lib/graph-rows'
import type { Graph } from '@/lib/types'

// 내 그래프 불러오기
export async function GET() {
  const supabase = await createClient()
  const {
    data: { user },
  } = await supabase.auth.getUser()
  if (!user) return NextResponse.json({ error: 'unauthorized' }, { status: 401 })

  // RLS가 자기 행만 보이게 하지만, 명시적으로 컬럼만 고른다.
  const { data: nodeRows } = await supabase.from('nodes').select('id,label,topic')
  const { data: edgeRows } = await supabase.from('edges').select('id,source,target')
  return NextResponse.json(rowsToGraph(nodeRows ?? [], edgeRows ?? []))
}

// 내 그래프 통째 저장(교체)
export async function PUT(request: Request) {
  const supabase = await createClient()
  const {
    data: { user },
  } = await supabase.auth.getUser()
  if (!user) return NextResponse.json({ error: 'unauthorized' }, { status: 401 })

  const graph = (await request.json()) as Graph

  // 통째 교체를 한 묶음으로: Postgres 함수(RPC)가 삭제+삽입을 전부 되거나 전부 취소되게 처리 → 반쪽 저장 차단.
  // user_id는 함수 안에서 auth.uid()로 채우므로 클라이언트는 점·선 데이터만 보낸다.
  const { error } = await supabase.rpc('replace_graph', {
    p_nodes: graph.nodes,
    p_edges: graph.edges,
  })
  if (error) return NextResponse.json({ error: error.message }, { status: 500 })

  return NextResponse.json({ ok: true })
}
```

- [ ] **Step 2: 타입체크**

Run: `npx tsc --noEmit`
Expected: 통과

- [ ] **Step 3: 커밋**

```bash
git add src/app/api/graph/route.ts
git commit -m "feat: 그래프 조회·저장 api 라우트 (#N)"
```

> 동작 검증은 인증이 붙은 Task 5~6 이후 Task 8 수동 검수에서 한다(지금은 타입만).

---

### Task 5: 인증 콜백 라우트 + 로그인 버튼

**Files:**
- Create: `src/app/auth/callback/route.ts`, `src/components/AuthButton.tsx`

- [ ] **Step 1: OAuth 콜백 라우트**

`src/app/auth/callback/route.ts` (소셜 로그인 후 코드를 세션 쿠키로 교환):

```ts
import { NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'

export async function GET(request: Request) {
  const { searchParams, origin } = new URL(request.url)
  const code = searchParams.get('code')
  if (code) {
    const supabase = await createClient()
    await supabase.auth.exchangeCodeForSession(code) // 코드 → 세션 쿠키
  }
  return NextResponse.redirect(origin) // 홈으로
}
```

- [ ] **Step 2: 로그인/로그아웃 버튼**

`src/components/AuthButton.tsx`:

```tsx
'use client'
import { useEffect, useState } from 'react'
import { createClient } from '@/lib/supabase/client'

export function AuthButton() {
  const supabase = createClient()
  const [email, setEmail] = useState<string | null>(null)

  useEffect(() => {
    // 현재 로그인 상태 + 변화 구독
    supabase.auth.getUser().then(({ data }) => setEmail(data.user?.email ?? null))
    const { data: sub } = supabase.auth.onAuthStateChange((_event, session) => {
      setEmail(session?.user?.email ?? null)
    })
    return () => sub.subscription.unsubscribe()
    // supabase 인스턴스는 매 렌더 새로 만들어지지만 동작에 영향 없음 — 한 번만 구독
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [])

  async function login(provider: 'google' | 'kakao') {
    await supabase.auth.signInWithOAuth({
      provider,
      options: { redirectTo: `${location.origin}/auth/callback` },
    })
  }

  async function logout() {
    await supabase.auth.signOut()
  }

  if (email) {
    return (
      <span className="flex items-center gap-2 text-sm">
        <span className="text-gray-500">{email}</span>
        <button className="border px-2" onClick={logout}>
          로그아웃
        </button>
      </span>
    )
  }
  return (
    <span className="flex items-center gap-2">
      <button className="border px-2" onClick={() => login('google')}>
        Google 로그인
      </button>
      <button className="border px-2" onClick={() => login('kakao')}>
        Kakao 로그인
      </button>
    </span>
  )
}
```

- [ ] **Step 3: 타입체크**

Run: `npx tsc --noEmit`
Expected: 통과

- [ ] **Step 4: 커밋**

```bash
git add src/app/auth/callback/route.ts src/components/AuthButton.tsx
git commit -m "feat: 소셜 로그인 콜백·로그인 버튼 (#N)"
```

---

### Task 6: `storage.ts` 비동기 전환 + 로그인 분기

**Files:**
- Modify: `src/lib/storage.ts` (전면 교체)
- Test: `src/lib/storage.test.ts` (기존 `hasStoredGraph` 테스트 제거/교체)

> 기존 `storage.ts`는 localStorage 동기 버전이다. **원본 동작(JSON 내보내기/가져오기)은 보존**하고 저장/불러오기만 비동기·서버로 바꾼다.

- [ ] **Step 1: 새 storage 작성**

`src/lib/storage.ts` (전체 교체):

```ts
import type { Graph } from './types'
import { createClient } from './supabase/client'
import { EXAMPLE_GRAPH } from './example-graph'

// 로그인 여부. 로컬 세션만 확인(네트워크 없음) — 진짜 검증은 서버 라우트가 getUser()로 한다.
async function isLoggedIn(): Promise<boolean> {
  const supabase = createClient()
  const {
    data: { session },
  } = await supabase.auth.getSession()
  return Boolean(session)
}

// 그래프 불러오기: 로그인 → 서버 / 비로그인 → 예시 고정값
export async function loadGraph(): Promise<Graph> {
  if (await isLoggedIn()) {
    const res = await fetch('/api/graph')
    if (!res.ok) throw new Error('그래프 불러오기 실패')
    return (await res.json()) as Graph
  }
  return EXAMPLE_GRAPH // 비로그인 = 예시 데모
}

// 그래프 저장: 로그인 → 서버 통째 저장 / 비로그인 → no-op
export async function saveGraph(graph: Graph): Promise<void> {
  if (!(await isLoggedIn())) return // 비로그인은 저장하지 않음
  const res = await fetch('/api/graph', {
    method: 'PUT',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(graph),
  })
  if (!res.ok) throw new Error('그래프 저장 실패')
}

// 그래프 → 보기 좋은 JSON (파일 내보내기용)
export function exportJSON(graph: Graph): string {
  return JSON.stringify(graph, null, 2)
}

// JSON → 그래프 (파일 가져오기용, 호출 쪽에서 try/catch)
export function importJSON(json: string): Graph {
  return JSON.parse(json) as Graph
}
```

- [ ] **Step 2: storage 테스트 갱신**

`src/lib/storage.test.ts` — 기존 `hasStoredGraph`/localStorage 테스트가 있으면 제거하고, 분기 동작을 mock으로 검증:

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest'

// supabase 클라이언트 mock: getSession이 돌려줄 값을 테스트마다 바꾼다.
const getSession = vi.fn()
vi.mock('./supabase/client', () => ({
  createClient: () => ({ auth: { getSession } }),
}))

import { loadGraph, saveGraph } from './storage'
import { EXAMPLE_GRAPH } from './example-graph'

beforeEach(() => {
  getSession.mockReset()
  vi.stubGlobal('fetch', vi.fn())
})

describe('loadGraph', () => {
  it('비로그인이면 예시 그래프를 돌려준다(서버 호출 없음)', async () => {
    getSession.mockResolvedValue({ data: { session: null } })
    const graph = await loadGraph()
    expect(graph).toEqual(EXAMPLE_GRAPH)
    expect(fetch).not.toHaveBeenCalled()
  })

  it('로그인이면 /api/graph에서 불러온다', async () => {
    getSession.mockResolvedValue({ data: { session: { user: { id: 'u1' } } } })
    const server: { nodes: []; edges: [] } = { nodes: [], edges: [] }
    ;(fetch as ReturnType<typeof vi.fn>).mockResolvedValue({ ok: true, json: async () => server })
    const graph = await loadGraph()
    expect(fetch).toHaveBeenCalledWith('/api/graph')
    expect(graph).toEqual(server)
  })
})

describe('saveGraph', () => {
  it('비로그인이면 저장하지 않는다(no-op)', async () => {
    getSession.mockResolvedValue({ data: { session: null } })
    await saveGraph({ nodes: [], edges: [] })
    expect(fetch).not.toHaveBeenCalled()
  })

  it('로그인이면 PUT으로 저장한다', async () => {
    getSession.mockResolvedValue({ data: { session: { user: { id: 'u1' } } } })
    ;(fetch as ReturnType<typeof vi.fn>).mockResolvedValue({ ok: true, json: async () => ({ ok: true }) })
    await saveGraph({ nodes: [], edges: [] })
    expect(fetch).toHaveBeenCalledWith('/api/graph', expect.objectContaining({ method: 'PUT' }))
  })
})
```

- [ ] **Step 3: 테스트 실행**

Run: `npx vitest run src/lib/storage.test.ts`
Expected: PASS (4개)

- [ ] **Step 4: 타입체크** (page.tsx가 아직 동기 호출이라 에러가 날 수 있음 → Task 7에서 해결. storage·테스트만 먼저 통과 확인)

Run: `npx vitest run`
Expected: graph-rows·storage 테스트 통과

- [ ] **Step 5: 커밋**

```bash
git add src/lib/storage.ts src/lib/storage.test.ts
git commit -m "refactor: storage 비동기 전환 + 로그인 분기 (#N)"
```

---

### Task 7: `page.tsx`·`Controls` 통합 (비동기 로드·디바운스 저장·로그인 UI)

**Files:**
- Modify: `src/app/page.tsx`
- Modify: `src/components/Controls.tsx` (또는 page에 `AuthButton` 배치)

- [ ] **Step 1: page.tsx의 저장/복원 effect 교체**

기존 두 effect(첫 로드 분기 + 변경 시 저장)를 아래로 교체. `hasStoredGraph`·`EXAMPLE_GRAPH` 직접 분기 로직은 제거(이제 `loadGraph`가 내부에서 판단). import에서 `hasStoredGraph` 제거.

```tsx
// import 수정: hasStoredGraph 제거, 나머지 유지
import { saveGraph, loadGraph } from '@/lib/storage'
import { createClient } from '@/lib/supabase/client'
import { AuthButton } from '@/components/AuthButton'
```

```tsx
// 첫 로드 + 로그인 상태 변화 시: 알맞은 저장소에서 그래프를 불러온다
useEffect(() => {
  const supabase = createClient()

  function refresh() {
    loadGraph()
      .then((graph) => setAll(graph.nodes, graph.edges))
      .catch(() => {}) // 실패 시 화면 유지
  }

  refresh() // 첫 로드
  const { data: sub } = supabase.auth.onAuthStateChange(() => refresh()) // 로그인/로그아웃 시 다시
  return () => sub.subscription.unsubscribe()
}, [setAll])

// 변경 시: 디바운스(0.6초) 후 저장. 비로그인이면 saveGraph가 no-op.
useEffect(() => {
  const timer = setTimeout(() => {
    saveGraph({ nodes, edges }).catch(() => {})
  }, 600)
  return () => clearTimeout(timer)
}, [nodes, edges])
```

> 참고: 불러온 직후에도 저장 effect가 한 번 도는데, 로그인 시 같은 데이터를 다시 PUT할 뿐이라 무해하다(필요해지면 "방금 불러옴" 플래그로 건너뛰기 — 지금은 생략).

- [ ] **Step 2: 로그인 버튼 배치**

`page.tsx`의 `<main>` 최상단(또는 `Controls` 옆)에 로그인 UI를 둔다:

```tsx
<div className="flex justify-end">
  <AuthButton />
</div>
```

- [ ] **Step 3: 타입체크 + 빌드**

Run: `npx tsc --noEmit && npm run build`
Expected: 통과

- [ ] **Step 4: 커밋**

```bash
git add src/app/page.tsx src/components/Controls.tsx
git commit -m "feat: 화면을 서버 저장·로그인 흐름에 연결 (#N)"
```

---

### Task 8: 수동 검수 (진짜 게이트) + 배포 환경변수

**Files:** 없음(검수) · Vercel 환경변수(사용자)

- [ ] **Step 1: 로컬 비로그인 확인**

Run: `npm run dev` → 로그인 안 한 상태에서 예시 그래프가 보이고, 점을 추가해도 **새로고침하면 예시로 리셋**되는지(저장 안 됨) 확인.

- [ ] **Step 2: 로컬 로그인 왕복 확인**

Google(또는 Kakao) 로그인 → 콜백 후 홈 복귀 → 점 몇 개 추가 → 새로고침 → **그대로 남아 있는지**(서버 저장) 확인. 로그아웃하면 예시로 돌아오는지.

- [ ] **Step 3: 멀티기기(또는 다른 브라우저) 확인**

다른 브라우저(또는 시크릿 창)에서 같은 계정으로 로그인 → **같은 그래프**가 보이는지 확인. (이게 이번 마일스톤의 핵심 가치)

- [ ] **Step 4: DB에 실제로 들어갔는지 확인(증거)**

Supabase 대시보드 → Table Editor → `nodes`/`edges`에 내 `user_id` 행이 쌓였는지 눈으로 확인.

- [ ] **Step 5: 배포 환경변수 등록** (사용자)

Vercel 프로젝트 → Settings → Environment Variables에 `NEXT_PUBLIC_SUPABASE_URL`·`NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` 등록 → 재배포. 각 소셜 제공자 콘솔의 redirect URI에 **배포 도메인**도 추가. 배포본에서 로그인 → 저장 왕복 재확인.

- [ ] **Step 6: PR + Kimi 리뷰 + 사용자 머지**

`/commit-push`로 PR 생성 → Kimi 코드리뷰 → 받은 지적 처리 → **사용자가 직접 머지**. 계획 체크박스 `- [x]` 갱신 동봉.

---

## 후속 / 미루는 것 (별도 이슈)

- **이메일 회원가입(2차)** — 소셜 로그인 골격 검증 후 얹기(이메일 인증·비밀번호 재설정).
- **"비우기" 버튼 비로그인 동작** — 로그인 시 = 서버 비우기. 비로그인 시 동작은 구현 중 확정(숨기거나 화면만 초기화).
- **저장 충돌·실시간 동기화·공유** — ADR 0011에서 범위 밖으로 분리.
- **불러온 직후 중복 저장 방지 플래그** — 무해해서 지금은 생략.
